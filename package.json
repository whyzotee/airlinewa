{
  "name": "airlinewa",
  "type": "module",
  "scripts": {
    "web": "cd client && bun i && bun run dev",
    "api": "cd server && fastapi dev main.py"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@base-ui-components/react": "^1.0.0-alpha.6"
  }
}
